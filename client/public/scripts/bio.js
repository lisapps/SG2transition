(function($){
	/*
	#################################
	|| Disabled for future release ||
	#################################
	var bioSelect = document.getElementById('bioSearchDrop').firstElementChild;
	bioSelect.addEventListener('click', function(evt) {
		var _target = this.parentNode;
		var _inputs = _target.getElementsByTagName('input');
		_target.classList.toggle('c-searchDrop--active');
		if(_target.classList.contains('c-searchDrop--active')) {
			for(var i = 0; i < _inputs.length; i++) {
				_inputs[i].value = '';
				if(i==0) _inputs[i].focus();
			}
		}
	})
	*/
})(jQuery)